apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "build-pipeline.fullname" . }}-test-connection"
  labels:
    {{- include "build-pipeline.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "build-pipeline.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
