apiVersion: v1
kind: Service
metadata:
  name: {{ include "build-pipeline.fullname" . }}
  labels:
    {{- include "build-pipeline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "build-pipeline.selectorLabels" . | nindent 4 }}
